import { css } from 'styled-components/macro';

export default css`
  --app-header-padding: var(--scale-rhythm);
  --app-header-background-color: var(--foreground-color);
  --app-header-shadow: var(--shadow-default);
`;
